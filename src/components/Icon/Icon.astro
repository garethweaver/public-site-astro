---
const { className, use } = Astro.props

export interface Props {
  className?: string,
  use: string,
}
---

<svg height="24" width="24" class={className}>
  <use href={`/icons/${use}.svg#icon`} />
</svg>
